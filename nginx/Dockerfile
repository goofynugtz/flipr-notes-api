FROM nginx
COPY ./default.conf /etc/nginx/conf.d/default.conf
# COPY ./ssl.sh ./ssl.sh
# RUN apt-get update
# RUN apt-get install python3-certbot-nginx -y
# RUN sudo certbot certonly --standalone --preferred-challenges http -d example.com
# RUN pwd
# RUN ls -la
# ENTRYPOINT [ "bash","./ssl.sh" ]
